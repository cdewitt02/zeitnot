# Discussions is deliberately not enabled — see
# docs/opensource-readiness/02-open-questions.md Q6. One inbox that gets
# answered beats two that do not.
blank_issues_enabled: true
contact_links:
  - name: Setup not working?
    url: https://github.com/cdewitt02/zeitnot/blob/main/docs/troubleshooting.md
    about: Run `zeitnot doctor` first — it reports every failing check at once. The troubleshooting guide covers the failures that look like bugs but are not.
